<%= form_with(model: beer_club) do |form| %>
  <% if beer_club.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
        <% beer_club.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <label for="name" class="form-label">Name</label>
    <input type="text" class="form-control" name="beer_club[name]">
  </div>

  <div>
    <label for="name" class="form-label">Founded</label>
    <input type="number" class="form-control" name="beer_club[founded]">
  </div>

  <div>
    <label for="name" class="form-label">City</label>
    <input type="text" class="form-control" name="beer_club[city]">
  </div>

  <div>
    <%= form.submit "Create beer club", class:"btn btn-primary" %>
  </div>
<% end %>
